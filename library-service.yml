zuul:
  routes:
    book-service:
      path: /bookservice/**
      service-id: book-service
    catalog-service:
      path: /catalogservice/**
      service-id: catalog-service
  host:
      socket-timeout-millis: 300000
